// module.exports = function (language) {
//   console.log('we need to get right of language selection');
//   if (language === 'es') { return { isEs: true }; }
//   if (language === 'cat') { return { isCat: true }; }
//   if (language === 'en') { return { isEn: true }; }
// };
